"use strict";

let tekst = "naIMA";

let tekst1 = tekst[0].toUpperCase();
let tekst2 = tekst.substring(1).toLowerCase();

let output = tekst1 + tekst2;

console.log(output);

//del2.......

function writeCapitalized(fornavn) {
  console.log(`${fornavn}`);
}

