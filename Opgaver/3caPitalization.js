"use strict";

let tekst = "nAIma";

let tekst1 = tekst[2].toUpperCase();
let tekst2 = tekst.substring(0, 2).toLowerCase();
let tekst3 = tekst.substring(3, 5).toLowerCase();

let output = tekst2 + tekst1 + tekst3;

console.log(output);
